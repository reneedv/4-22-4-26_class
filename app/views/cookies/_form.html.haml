= form_for @cooky do |f|
  - if @cooky.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cooky.errors.count, "error")} prohibited this cooky from being saved:"
      %ul
        - @cooky.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :rescipe
    = f.text_field :rescipe
  .actions
    = f.submit 'Save'
