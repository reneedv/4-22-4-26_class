= form_for @cookie, :html => { :class => 'form-horizontal' } do |f|
  .control-group
    = f.label :rescipe, :class => 'control-label'
    .controls
      = f.text_field :rescipe, :class => 'text_field'
  .form-actions
    = f.submit nil, :class => 'btn btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), cookies_path, :class => 'btn'
